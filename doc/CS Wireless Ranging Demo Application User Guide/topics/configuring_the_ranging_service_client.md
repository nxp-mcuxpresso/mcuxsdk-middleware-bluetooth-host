# Configuring the ranging service client

Connect to the board that acts as the ranging service client \(the board that initiates the measurement request\) using a serial console \(see [Firmware setup](firmware_set-up.md)\).

Run the following command:

```
communication role {central | peripheral}
```

**Parent topic:**[Using the console](../topics/using_the_console.md)

